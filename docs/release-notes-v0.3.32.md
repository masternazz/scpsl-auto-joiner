# SCP:SL Auto-Joiner v0.3.32

Adds the separately packaged optional LabAPI companion for owned or authorized servers. It provides authenticated status, round, capacity, and explicitly allowed personal-role data, with loopback defaults, rate limiting, token protection, and safe A2S fallback.

The companion requires the target server's compatible LabAPI assembly and is never installed automatically.
